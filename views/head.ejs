<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= title %></title>
    <style>
      body {
        margin: 0;
        min-height: 100vh;
        background: #ffffff;
        font: 14px/1.5 "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
          Arial, sans-serif;
        color: #142038;
      }
      main {
        width: min(1100px, 92vw);
        margin: 0 auto;
        padding: clamp(2.4rem, 6vw, 4.8rem) 0 clamp(3rem, 7vw, 5rem);
      }
      h1 {
        font-size: clamp(2.1rem, 4.6vw, 2.8rem);
        margin-bottom: 1rem;
        font-weight: 700;
        color: #0d172b;
      }
      p {
        margin: 0 0 1.2rem;
        color: #394457;
      }
      .error {
        color: #9b1b1b;
        background: rgba(214, 69, 65, 0.14);
        padding: 0.9rem 1.1rem;
        border-radius: 12px;
        border: 1px solid rgba(214, 69, 65, 0.28);
      }
      .success {
        color: #0f5132;
        background: rgba(60, 180, 110, 0.18);
        padding: 0.9rem 1.1rem;
        border-radius: 12px;
        border: 1px solid rgba(60, 180, 110, 0.3);
      }
      .surface-muted {
        margin: 2.2rem 0;
        padding: clamp(1.4rem, 4vw, 2rem);
        border-radius: 18px;
        background: linear-gradient(135deg, rgba(244, 245, 250, 0.95), rgba(255, 255, 255, 0.95));
        border: 1px solid rgba(20, 32, 52, 0.05);
        box-shadow: 0 18px 40px rgba(18, 35, 66, 0.12);
      }
    </style>
  </head>
  <body>
